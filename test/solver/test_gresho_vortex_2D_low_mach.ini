[run]
solver_name=Hydro_Muscl_2D
tEnd=1.0
nStepmax=10000

# noutput equals -1 means we dump data at every time steps
nOutput=10

[amr]
level_min=7
level_max=7

# init step may contain calls to globalRefine
# if the following option is true, we interleave
# calls to load balancing in between globalRefine
# if the following option is false, a single load
# balancing operation is done at the end of
# globalRefine steps
enable_load_balance_during_init=false

epsilon_refine=0.01
epsilon_coarsen=0.002

[mesh]
xmin=0.0
xmax=1.0

ymin=0.0
ymax=1.0

# periodic border condition
boundary_type_xmin=3
boundary_type_xmax=3

boundary_type_ymin=3
boundary_type_ymax=3

[hydro]
gamma0=1.666
cfl=0.8
niter_riemann=10
iorder=2
slope_type=2
problem=gresho_vortex
riemann=llf

[Gresho]
# density
rho0 = 1.0
# Mach number
Ma = 0.01

[low_mach]
rsst_enabled=yes
rsst_ksi=10.0

[output]
outputDir=./
outputPrefix=test_gresho_vortex_2D_low_mach
outputVtkAscii=false
write_variables=rho,rho_vx,rho_vy,e_tot,velocity
vtk_enabled=no
hdf5_enabled=yes
output_debug=yes

[other]
implementationVersion=0

